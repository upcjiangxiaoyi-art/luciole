# Luciole 2.0 架构决策记录（回答交接书十问）

> 施工者：波哥（Claude Fable 5）· 2026-08-14
> 本版本：v2.0.0 Phase 1 —— 只交付「帷幕沙漏 · 均匀散落」闭环
> v3.3.0 补记（2026-08-21）：第二幕不接领航员，推进靠轮钟 + 手动；「到位」永不注入；习性单只生成不注入。三幕各自独立账本／通道／日志。
> v3.7.0 补记（2026-09-06）：第二幕接上领航员，**推翻 v3.3.0 的「不接」**。当时三条顾虑——省钱、可控、判错了不知道——不是反对领航员，是反对「每轮都问、说翻就翻、翻了不留痕」的领航员。这次的接法逐条对着它们：最少轮数门 + 重抽不重问（省钱）；轮钟降为上限、手动推进撤回照旧、默认仍是轮钟（可控）；每次 DONE 留痕带理由 + 弹提示 + 面板实时显示（判错了你知道）。「到位」仍然永不注入，只是多了领航员这一个读者。判与翻分开：判在两轮空档里，翻在下一条玩家消息——与第一幕智能调度同一条流水线，时序问题不重开。
> v3.8.0 补记（2026-09-06）：第三幕迷雾森林开张。它与前两幕的关系是**反的**：前两幕限制演员的信息，这一幕 user 和演员都是玩家，剧本在 God 手里。God 一次只发一处环境（地方／环境／在场／规矩四栏公开，出口条件只给 God 和你），每隔 N 轮在回复落地后的空档里判「出口条件落地了没」，备好的下一处在下一条玩家消息时换过去——仍是同一条一轮流水线。「上下文摘要」不另建：足迹里每一处离开时 God 记的一句 recap 就是摘要，下次发牌读它。四条通道、三本账本、三份日志，谁也不碰谁。
> v3.9.0 补记（2026-09-19）：第二幕多了一把导演椅——🎬 剧情步骤。幕本管「这一阶段往哪走」，拦得住抢演下一幕，拦不住把眼前一段戏一口气演完（「去登山」一层就登完回家）。步骤管「这一回合落到哪件事上」：玩家说想玩什么，模型被**强制先分析再切成 N 步**（N 玩家定），之后每一轮只把一步贴到演员耳边（固定 depth 0，不读深度设置），后面的步骤不在它上下文里。消费时序复用第一幕线索的「回复位」：贴上→随回复演出（重抽带着）→玩家再发言才换下一步，连发不吞。第五条注入通道、自己的账本、日志写进星灯日志、总电闸一并管。「想」的过程必须看得见：`readOpenAiStream` 的进度回调多传正文本身，【分析】段实时流到面板，JSON 出现即截住——这是这次唯一动到公共层的地方，老调用方只读前两个数字，不受影响。步骤是原样贴给演员的，与第一幕秘密做 6 字重合扫描（同「不准」）。走完自动撤下：它是一段戏的调度，不是常驻布景。
> v3.8.1 补记（2026-09-06）：连接也分家。三页各自的连接槽，**页内可留空复用主连接，跨页绝不借**——否则改一页的连接会悄悄改掉另一页的行为，「独立行走」就是假的。升级时把当时实际生效的连接抄进新槽（一次性），装完不哑。

## 十问十答

1. **酒馆有没有可靠的生成前异步闸？**
   不赌。均匀模式在 `MESSAGE_SENT` 事件里**同步**调用 `setExtensionPrompt`——本地操作零延迟，酒馆组装提示发生在事件处理之后，注入必定生效。这就是为什么 Phase 1 只做均匀模式：它从物理上绕开了整个时序坑。

2. **若无异步闸，采用哪种流水线？**
   Phase 1 不需要。未来智能调度将采用"上一轮结束后后台规划下一轮"的一轮流水线，绝不假装同轮阻塞。

3. **线索"成功"的定义？**
   注入进入了至少一次真实生成（`MESSAGE_RECEIVED` 计数 ≥ 1）。不做正文逐字命中验证——那是探针地狱的入口。

4. **排程在哪个节点消费？**
   线索占据一个完整"回复位"（含所有重抽）。下一条玩家消息到来时，若该线索已被至少一次生成使用，才退役并推进游标。玩家连发消息时线索原地等待，不空耗。

5. **三模式是否三内核？**
   是。本版只有均匀内核（纯本地队列+相对轮时钟）。智能调度与AI监督未来各建独立小运行器，共用隐藏资料格式与注入器，不共用状态机。

6. **MVP 需要几层几档？**
   零层零档。线索就是一个有序序列，从浅到深的顺序在编译期由强模型决定。力度（轻柔/标准/清晰）是编译期风格参数，不是运行期状态。

7. **哪些安全性可严格证明？**
   三层，全部本地可验证：① 结构安全——完整秘密永不进入注入文本；② 白名单——只注入编译产物；③ 字面安检——禁词表 + 残留宏拦截 + 与秘密原文的连续片段重合检查（≥12字滑窗）。不承诺"语义数学无菌"。

8. **1.14～1.17 与 1.18+ 分级？**
   均匀模式只依赖三个能力：`setExtensionPrompt`、`chatMetadata`、`MESSAGE_SENT/RECEIVED/CHAT_CHANGED` 事件——全部是 1.14 就有的老 API。启动时能力自检，缺哪个明说哪个。

9. **中间状态的唯一出口？**
   状态只有四个：空白 → 已编译 → 点亮 → 送完。线索只有三态：排队 → 台上 → 已送。任何时刻面板只显示一句人话 + 一个建议动作。没有 pending 锁，没有需要仲裁的中间态。

10. **核心代码行数？**
    Phase 1 均匀内核约 1300 行；三模式全交付后约 2100 行（v1.7 是 9054 行）。删掉的不是功能，是永远修不完的自证机制。

## 消费时机状态图

```
玩家发消息(MESSAGE_SENT)
 ├─ 台上有线索 且 已被生成用过 → 退役(used) + 清注入 + 游标++
 ├─ 台上有线索 但 从未被生成用过 → 原地保留（不浪费）
 ├─ 轮钟++
 └─ 台上无线索 且 到点 且 还有存货 → 下一条上台 + 注入 + 排程推进

AI回复落地(MESSAGE_RECEIVED)
 └─ 台上有线索 → 送达计数++（重抽自然复用同一条，不重复消费）

切换聊天(CHAT_CHANGED)
 ├─ 重载本聊天账本
 ├─ 台上线索已用过 → 补退役
 └─ 台上线索没用过 → 恢复注入（现场还原）
```

## 与旧版数据的关系

零迁移。存储键全新（`luciole_v2`），旧版数据原样留在 chatMetadata 里不读不动。用户卸载旧版、安装新版，从零开始一个故事。这是交接书 7.8 的直接执行：不再写解释旧 Bug 的迁移补丁。
